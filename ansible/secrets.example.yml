---
# Copy this file to secrets.yml and fill in real values, then encrypt:
#   ansible-vault encrypt secrets.yml
#
# Reference in playbooks with:
#   vars_files:
#     - "{{ playbook_dir }}/../secrets.yml"

# Tailscale authentication key (generate at https://login.tailscale.com/admin/settings/keys)
# Use a reusable, pre-approved auth key for automation
tailscale_auth_key: "tskey-auth-XXXXXXXXXXXXXXXXXXXX"
